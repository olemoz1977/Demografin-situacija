name: Update demographic data

on:
  workflow_dispatch:
  schedule:
    - cron: '15 6 1 * *'
  push:
    branches: [main]
    paths:
      - 'scripts/update_sex_ratio.py'
      - 'scripts/update_tfr_nowcast.py'
      - '.github/workflows/update-demography-data.yml'

permissions:
  contents: write

jobs:
  update-demography-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Fetch official Eurostat sex-age data
        run: python scripts/update_sex_ratio.py

      - name: Fetch official Eurostat 2025 TFR nowcast
        run: python scripts/update_tfr_nowcast.py

      - name: Commit updated data if changed
        shell: bash
        run: |
          if [[ -n "$(git status --porcelain data/)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/sex-ratio-history.json data/tfr-nowcast-2025.json
            git commit -m "Update Eurostat demographic data"
            git push
          else
            echo "No data changes"
          fi
